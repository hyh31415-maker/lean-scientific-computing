interface:
  display_name: "Lean Scientific Computing"
  short_description: "Build auditable research code without project bloat"
  default_prompt: "Use $lean-scientific-computing to implement this defined research computation through one auditable path, preserving any Probe Contract and adding only justified structure."
policy:
  allow_implicit_invocation: true
