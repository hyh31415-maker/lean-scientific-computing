interface:
  display_name: "Lean Scientific Computing"
  short_description: "Implement scientific evidence with one auditable code path"
  default_prompt: "Use $lean-scientific-computing to preserve the Probe Contract, implement the smallest direct computation, run meaningful scientific checks, and return a complete Evidence Record."
policy:
  allow_implicit_invocation: true
