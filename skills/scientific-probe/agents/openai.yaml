interface:
  display_name: "Scientific Probe"
  short_description: "Minimal experiments, not premature products"
  default_prompt: "Use $scientific-probe to treat this as a scientific inference task and minimize engineering overhead."
policy:
  allow_implicit_invocation: true
